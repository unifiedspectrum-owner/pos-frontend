/* Shared API route constants */

/* CSRF token API base path and endpoints */
export const CSRF_API_ROUTES = {
  BASE_PATH: '/csrf',
  GET_TOKEN: '/token',
} as const;

/* Countries API base path and endpoints */
export const COUNTRIES_API_ROUTES = {
  BASE_PATH: '/countries',
  GET_ALL: '',
} as const;

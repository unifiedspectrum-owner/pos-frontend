/* Support ticket management types module exports */

/* Ticket-related types (entities, requests, responses) */
export * from './ticket'

/* Communication-related types (entities, requests, responses) */
export * from './communication'

/* Attachment-related types (entities, responses) */
export * from './attachment'

/* Category-related types (entities, responses) */
export * from './category'

/* Assignment-related types (entities, requests, responses) */
export * from './assignment'
